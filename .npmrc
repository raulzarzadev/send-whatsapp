# pnpm configuration
shamefully-hoist=true
auto-install-peers=true

# Allow build scripts for these packages
enable-pre-post-scripts=true

# Trusted dependencies that can run scripts
# better-sqlite3 needs to compile native bindings
lifecycle-scripts-for[better-sqlite3]=true
lifecycle-scripts-for[@whiskeysockets/baileys]=true
